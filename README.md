**Instalar Dependecias:** 

*Back-End*
```bash
cd back-end

pip install requirements.txt
```
*Front-End*
```bash
cd front-end

pip install requirements.txt
```

**Inicialização do Flask:**
```bash
cd back-end

python -m flask --app app run
```
